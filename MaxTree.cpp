//构建数组的MaxTree
// MaxTree定义为一棵二叉树，其中的节点与数组元素一一对应，同时对于MaxTree的每棵子树，
//它的根的元素值为子树的最大值。
// ，对于数组中的每个元素，其在树中的父亲为数组中它左边比它大的第一个数和右边比它大的第一个数中更小的一个。
// 若两边都不存在比它大的数，那么它就是树根。
#include <vector>
#include <stack>
#include <string>
using namespace std;

//返回索引
vector<int> GetFirstGreater(vector<int> v)
{
    stack<int> s;
    string ss;
    ss.substr()
    vector<int> ret(v.size());
    for()
}
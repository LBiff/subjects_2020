//查找所有的最大值与最小值之差满足要求的子数组
//给定数组arr和整数num， 共返回有多少个子数组满足如下情况：
// max(arr[i..j]) - min(arr[i..j]) <= num
// max(arr[i..j])表示子数组arr[i..j]中的最大值， min(arr[i..j])表示子数组arr[i]中的最小值。
// 【 要求】如果数组长度为N， 请实现时间复杂度为O(N)的解法。 

//最大最小更新窗口结构的应用
